type STD @model @auth(rules: [{allow: public}]) {
  id: ID!
  state: String
  facepage: String
  masterapplication: String
  tableofcontents: String
  definitions: String
  premiums: String
}

type Note @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  image: String
}
 